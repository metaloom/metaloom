.DS_Store
target
.classpath
.project
.settings
.bin
.pmd*
*.orig
.index*
hs_err_*.log
.vertx
dependency-reduced-pom.xml
.checkstyle
.testcontainers*
.idea
.factorypath
*.iml
.vscode
__pycache__

# Written into whichever module a test JVM runs in, because both defaults are relative to the working
# directory: StorageOptions.DEFAULT_UPLOAD_DIRECTORY ("data/storage") and the Vert.x BodyHandler
# upload dir ("file-uploads"). Test output, never sources.
**/data/storage/
file-uploads
