{
  "guard": {
    "status": "pending",
    "pictures": [
      "config",
      "debug"
    ],
    "reason": "the node landed while these pictures were being taken and its descriptor is not staged into website/static/pipeline-editor/node-descriptors.json yet — the config panel is rendered from that file, so neither picture can be taken until it is. The debug view then needs a guardrail model (Llama Guard, ShieldGemma or Granite Guardian) answering for a real run",
    "unblockedBy": "stage the descriptor, add a guard recipe to DocsFixtureGenerator against a real guardrail model, then capture-node-config-screenshots.mjs && capture-node-screenshots.mjs guard"
  }
}
