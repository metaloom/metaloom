<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>cortex-vlm-node</artifactId>
    <packaging>jar</packaging>
    <parent>
        <groupId>io.metaloom.cortex</groupId>
        <artifactId>cortex-nodes</artifactId>
        <relativePath>../../pom.xml</relativePath>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <name>MetaLoom // Cortex :: Nodes :: VLM</name>

    <!--
        No extra runtime dependency on purpose: the image pre-processing uses plain
        ImageIO / Graphics2D and the HTTP call uses java.net.http, so this node stays
        free of the OpenCV native runtime and runs anywhere.
    -->
    <dependencies>
        <!--Test -->
        <dependency>
            <groupId>io.metaloom.utils</groupId>
            <artifactId>genai-utils-mock-llm-server</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
