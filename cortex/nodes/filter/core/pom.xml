<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>cortex-filter-node</artifactId>
    <packaging>jar</packaging>
    <parent>
        <groupId>io.metaloom.cortex</groupId>
        <artifactId>cortex-nodes</artifactId>
        <relativePath>../../pom.xml</relativePath>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <name>MetaLoom // Cortex :: Nodes :: Filter</name>

    <dependencies>
        <dependency>
            <groupId>io.metaloom.utils</groupId>
            <artifactId>genai-utils</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>
        <!-- The shared @Provides LLMProvider. See LLMProviderModule for why it is not declared here. -->
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-llm-common</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

</project>
