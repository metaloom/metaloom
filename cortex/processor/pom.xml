<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>cortex-processor</artifactId>
    <packaging>jar</packaging>

    <parent>
        <groupId>io.metaloom.cortex</groupId>
        <artifactId>cortex</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <name>MetaLoom // Cortex :: Processor</name>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.metaloom</groupId>
                <artifactId>bom</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>io.metaloom.utils</groupId>
            <artifactId>differential-filesystem-scanner</artifactId>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-common</artifactId>
        </dependency>
        <dependency>
            <groupId>me.tongfei</groupId>
            <artifactId>progressbar</artifactId>
        </dependency>

        <!-- Sources -->
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-filesystem-source-node</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Actions -->
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-hash-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-thumbnail-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-facedetect-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-scene-detection-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-llm-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-tika-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-ocr-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-loom-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-captioning-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-dedup-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-fingerprint-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-consistency-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-quality-node</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-whisper-node</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Pipeline -->
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-pipeline-core</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-pipeline-core</artifactId>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-common</artifactId>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.metaloom.cortex</groupId>
            <artifactId>cortex-core-media</artifactId>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>