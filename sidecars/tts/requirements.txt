# Cortex TTS sidecar. Python 3.11+ recommended.
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
httpx>=0.27
numpy>=1.26
soundfile>=0.13

# German — Orpheus/Kartoffel + SNAC decode (transformers backend). With
# ORPHEUS_BACKEND=vllm the LM runs on a separate vLLM/llama.cpp server and only
# snac + torch are needed here for the decode step.
transformers>=4.50
torch>=2.6
snac>=1.2.1
accelerate>=1.0

# English — Kokoro
kokoro-onnx==0.5.0
