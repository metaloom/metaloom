# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.library``."""

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

from .base import CreatorEditorResponse, ListResponse, Model


@dataclass
class LibraryAssetRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.library.LibraryAssetRequest``."""

    #: Uuid of the asset to add to the library.
    asset_uuid: str | None = None


@dataclass
class LibraryCreateRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.library.LibraryCreateRequest``."""

    #: The name of the library.
    name: str | None = None
    #: Storage pool for binaries uploaded into this library. Omit to use the server's local upload directory.
    #: The pool decides filesystem vs S3.
    pool_uuid: str | None = None
    #: Additional custom meta properties for the element.
    meta: dict[str, Any] | None = None


@dataclass
class LibraryListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.library.LibraryListResponse``."""

    #: Array which contains the found elements.
    data: list[LibraryResponse] = field(default_factory=list)


@dataclass
class LibraryResponse(CreatorEditorResponse):
    """Mirrors ``io.metaloom.loom.rest.model.library.LibraryResponse``."""

    #: The name of the library
    name: str | None = None
    #: The storage pool binaries uploaded into this library are written to. Absent when the library uses the
    #: server's local upload directory.
    pool_uuid: str | None = None
    #: Which backend this library stores binaries in: 'filesystem' or 's3'. Derived from the pool, so it is
    #: never null even when no pool is set.
    storage_type: str | None = None


@dataclass
class LibraryUpdateRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.library.LibraryUpdateRequest``."""

    #: The name of the library.
    name: str | None = None
    #: Storage pool for binaries uploaded into this library. Only affects binaries uploaded after the change;
    #: bytes already stored are not migrated.
    pool_uuid: str | None = None
    #: Additional custom meta properties for the element.
    meta: dict[str, Any] | None = None
