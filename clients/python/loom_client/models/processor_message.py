# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.processor.message``."""

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

from .base import Model
from .enums import NodeRegistrationRejectionReason, ProcessorCapability, ProcessorMessageType


@dataclass
class NodeRegistration(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.NodeRegistration``."""

    cortex_id: str | None = None
    #: NOTE: opaque: node descriptor (loom-shared/node-model)
    nodes: list[dict[str, Any]] = field(default_factory=list)


@dataclass
class NodeRegistrationAck(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.NodeRegistrationAck``."""

    #: The worker this acknowledgement is for
    cortex_id: str | None = None
    #: Node type ids whose contract Loom now serves on this worker's behalf
    accepted: list[str] = field(default_factory=list)
    #: Node type ids that were not adopted, each with a reason
    rejected: list[NodeRegistrationRejection] = field(default_factory=list)


@dataclass
class NodeRegistrationRejection(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.NodeRegistrationRejection``."""

    #: The node type id that was rejected
    node_id: str | None = None
    #: Machine-readable rejection reason
    reason: NodeRegistrationRejectionReason | None = None
    #: Human-readable explanation, naming the offending field where there is one
    message: str | None = None


@dataclass
class NodeTaskResultMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.NodeTaskResultMessage``."""

    #: The pipeline run; null for an untracked execution
    run_uuid: str | None = None
    #: The media item this result belongs to
    item_id: str | None = None
    #: The node outcome
    #: NOTE: opaque: node task payload (loom-shared/pipeline-model)
    result: dict[str, Any] | None = None


@dataclass
class ProcessorMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.ProcessorMessage``."""

    #: The type of message
    type: ProcessorMessageType | None = None
    #: The message payload, structure depends on type
    body: dict[str, Any] | None = None


@dataclass
class ProcessorRegistration(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.ProcessorRegistration``."""

    #: Unique identifier of the processor node
    node_id: str | None = None
    #: Human readable name of the processor node
    name: str | None = None
    #: Priority of the processor node (higher = preferred)
    priority: int = 0
    #: Host address of the processor node (e.g. 10.0.1.10:9090)
    host: str | None = None
    #: Capabilities offered by this processor node
    capabilities: list[ProcessorCapability] = field(default_factory=list)
    node_whitelist: list[str] = field(default_factory=list)
    node_blacklist: list[str] = field(default_factory=list)


@dataclass
class SourceCompleteMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.SourceCompleteMessage``."""

    run_uuid: str | None = None
    #: Total number of items emitted, for reconciliation against what was received
    total_count: int = 0
    #: Failure detail when enumeration aborted; null on success
    error: str | None = None


@dataclass
class SourceItemsAckMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.SourceItemsAckMessage``."""

    run_uuid: str | None = None
    #: Sequence number of the batch being acknowledged
    seq: int = 0


@dataclass
class SourceItemsMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.SourceItemsMessage``."""

    #: The pipeline run these items belong to
    run_uuid: str | None = None
    #: Monotonic batch sequence number, starting at 0
    seq: int = 0
    #: The discovered media items - references, never content
    #: NOTE: opaque: media reference (loom-shared/pipeline-model)
    items: list[dict[str, Any]] = field(default_factory=list)


@dataclass
class SourceTaskMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.SourceTaskMessage``."""

    #: The pipeline run this source task belongs to
    run_uuid: str | None = None
    #: Id of the source node within the pipeline graph
    node_id: str | None = None
    #: Node kind to run, e.g. filesystem-source
    node_kind: str | None = None
    #: Per-node options from the pipeline definition, e.g. path or pathGlobs
    options: dict[str, Any] = field(default_factory=dict)
    #: How many items the processor should send per batch
    batch_size: int = 0


@dataclass
class TaskReturnedMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.message.TaskReturnedMessage``."""

    #: The pipeline run the task belongs to
    run_uuid: str | None = None
    #: The media item the task was dispatched for
    item_id: str | None = None
    #: The dispatched task, for correlation with the task row
    task_uuid: str | None = None
    node_ids: list[str] = field(default_factory=list)
    #: Which element of a fanned-out sequence; 0 for a node that runs once per item
    element_seq: int = 0
    #: Why the worker gave the task back, recorded on the re-dispatch
    reason: str | None = None
