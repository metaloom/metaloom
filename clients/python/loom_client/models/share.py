# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.share``."""

from __future__ import annotations

from dataclasses import dataclass, field

from .base import CreatorEditorResponse, ListResponse, MetaModel, Model, Response


@dataclass
class ShareAnnotationListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareAnnotationListResponse``."""

    #: Array which contains the found elements.
    data: list[ShareAnnotationResponse] = field(default_factory=list)


@dataclass
class ShareAnnotationRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareAnnotationRequest``."""

    #: The asset being marked. Must be a member of the share.
    asset_uuid: str | None = None
    #: TEMPORAL, SPATIAL or SPATIOTEMPORAL. Must match the geometry supplied.
    kind: str | None = None
    #: Start in seconds from the beginning of the media. Required for TEMPORAL and SPATIOTEMPORAL.
    time_from: float | None = None
    #: End in seconds. Omit to mark a single moment rather than a range.
    time_to: float | None = None
    #: Left edge of the region, 0..1 of the media width. Required for SPATIAL and SPATIOTEMPORAL.
    area_x: float | None = None
    #: Top edge of the region, 0..1 of the media height. Required for SPATIAL and SPATIOTEMPORAL.
    area_y: float | None = None
    #: Region width as a fraction of the media width, greater than 0 and at most 1.
    area_width: float | None = None
    #: Region height as a fraction of the media height, greater than 0 and at most 1.
    area_height: float | None = None
    #: What the visitor wants to say about the marked part.
    text: str | None = None


@dataclass
class ShareAnnotationResponse(Response):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareAnnotationResponse``."""

    asset_uuid: str | None = None
    #: TEMPORAL, SPATIAL or SPATIOTEMPORAL.
    kind: str | None = None
    time_from: float | None = None
    time_to: float | None = None
    #: Left edge of the region, 0..1 of the media width.
    area_x: float | None = None
    #: Top edge of the region, 0..1 of the media height.
    area_y: float | None = None
    area_width: float | None = None
    area_height: float | None = None
    text: str | None = None
    #: The visitor name as it stood when the mark was drawn.
    author_name: str | None = None
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    created: float | None = None
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    edited: float | None = None


@dataclass
class ShareChallengeResponse(Response):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareChallengeResponse``."""

    #: What is behind the link: ASSET or COLLECTION. Enough for the viewer to pick a layout before it can load
    #: anything.
    target_type: str | None = None
    #: Whether a password must be supplied to open the link.
    password_required: bool | None = None
    #: Whether somebody has already named this link. When true the viewer skips the name question and reuses
    #: the stored name.
    visitor_name_known: bool | None = None
    #: The stored visitor name, when one exists. Shown back as a greeting rather than asked for again.
    visitor_name: str | None = None


@dataclass
class ShareCommentListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareCommentListResponse``."""

    #: Array which contains the found elements.
    data: list[ShareCommentResponse] = field(default_factory=list)


@dataclass
class ShareCommentRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareCommentRequest``."""

    asset_uuid: str | None = None
    #: The comment being replied to. Replies are one level deep; replying to a reply attaches to its parent.
    parent_uuid: str | None = None
    #: The mark on the media this comment belongs to, when anchoring it to one.
    annotation_uuid: str | None = None
    #: The comment text.
    text: str | None = None


@dataclass
class ShareCommentResponse(Response):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareCommentResponse``."""

    #: The asset this comment is about. Null when it addresses the shared collection as a whole.
    asset_uuid: str | None = None
    #: The comment this one replies to, when it is a reply.
    parent_uuid: str | None = None
    #: The mark on the media this comment belongs to, when it is anchored to one.
    annotation_uuid: str | None = None
    text: str | None = None
    #: The visitor name as it stood when the comment was written. Not a user reference - nobody here has an
    #: account.
    author_name: str | None = None
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    created: float | None = None
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    edited: float | None = None


@dataclass
class ShareCreateRequest(MetaModel):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareCreateRequest``."""

    #: What to share: ASSET or COLLECTION.
    target_type: str | None = None
    #: The uuid of the asset or collection to share. The caller must be allowed to read it.
    target_uuid: str | None = None
    #: Password for the link, in clear. Omit or send null for an open link. Stored hashed and echoed back
    #: exactly once.
    password: str | None = None
    #: When the link should stop working. Omit or send null for a link that never expires.
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    expires_at: float | None = None
    #: Whether the visitor may download the original file. Defaults to true.
    allow_download: bool | None = None
    #: Whether the visitor sees title, description, size, duration and dimensions. Defaults to true.
    show_metadata: bool | None = None
    #: Whether the visitor may leave comments. Defaults to false.
    allow_comments: bool | None = None
    #: Whether the visitor may react. Defaults to false.
    allow_reactions: bool | None = None
    #: Whether the visitor may mark regions and timecodes. Defaults to false.
    allow_annotations: bool | None = None


@dataclass
class ShareFeedbackResponse(Response):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareFeedbackResponse``."""

    #: The name the visitor gave when they first opened the link.
    visitor_name: str | None = None
    comments: list[ShareCommentResponse] = field(default_factory=list)
    annotations: list[ShareAnnotationResponse] = field(default_factory=list)
    reactions: list[ShareReactionResponse] = field(default_factory=list)


@dataclass
class ShareListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareListResponse``."""

    #: Array which contains the found elements.
    data: list[ShareResponse] = field(default_factory=list)


@dataclass
class ShareReactionListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareReactionListResponse``."""

    #: Array which contains the found elements.
    data: list[ShareReactionResponse] = field(default_factory=list)


@dataclass
class ShareReactionRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareReactionRequest``."""

    #: APPROVE, REJECT, THUMBSUP, THUMBSDOWN, LOVE or QUESTION. Its own vocabulary, not the internal
    #: ReactionType.
    type: str | None = None
    #: The asset being reacted to. Must be a member of the share.
    asset_uuid: str | None = None
    #: The guest comment being reacted to.
    comment_uuid: str | None = None
    #: The guest annotation being reacted to.
    annotation_uuid: str | None = None


@dataclass
class ShareReactionResponse(Response):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareReactionResponse``."""

    type: str | None = None
    asset_uuid: str | None = None
    comment_uuid: str | None = None
    annotation_uuid: str | None = None
    #: The visitor name as it stood when the reaction was left.
    author_name: str | None = None
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    created: float | None = None


@dataclass
class ShareResponse(CreatorEditorResponse):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareResponse``."""

    #: The public half of the link. Append it to the share URL to open the customer view.
    slug: str | None = None
    #: Absolute URL of the customer view, assembled from the request's own host so it is pasteable as-is.
    url: str | None = None
    #: What is shared: ASSET or COLLECTION.
    target_type: str | None = None
    #: The uuid of the shared asset or collection.
    target_uuid: str | None = None
    #: Human readable name of the shared asset or collection, so a list of links is readable without a second
    #: request.
    target_name: str | None = None
    #: The link password in clear. Returned only once, in the response to the request that set it - it is
    #: stored hashed.
    password: str | None = None
    #: Whether opening this link requires a password.
    password_protected: bool | None = None
    #: Whether the expiry date has passed. A lapsed link answers 404 to its visitors.
    expired: bool | None = None
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    expires_at: float | None = None
    allow_download: bool | None = None
    show_metadata: bool | None = None
    allow_comments: bool | None = None
    allow_reactions: bool | None = None
    allow_annotations: bool | None = None
    #: The name the first visitor gave. Null until somebody opens the link.
    visitor_name: str | None = None
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    first_visited_at: float | None = None
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    last_viewed_at: float | None = None
    #: How many times the link has been opened. Counted once per visit, not per request.
    view_count: int | None = None
    #: How many comments, annotations and reactions the visitor has left.
    feedback_count: int | None = None


@dataclass
class ShareSessionRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareSessionRequest``."""

    #: The link password. Required when the challenge reported passwordRequired.
    password: str | None = None
    visitor_name: str | None = None


@dataclass
class ShareSessionResponse(Response):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareSessionResponse``."""

    session_token: str | None = None
    #: When the session token stops being accepted. The link's own expiry, when earlier, still wins.
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    session_expires_at: float | None = None
    #: The name this link is known by - the one just given, or the one stored from an earlier visit.
    visitor_name: str | None = None
    #: What is behind the link: ASSET or COLLECTION.
    target_type: str | None = None
    #: Name of the shared asset or collection, for the viewer's heading.
    target_name: str | None = None
    #: Description of the shared collection, when it has one.
    target_description: str | None = None
    allow_download: bool | None = None
    show_metadata: bool | None = None
    allow_comments: bool | None = None
    allow_reactions: bool | None = None
    allow_annotations: bool | None = None


@dataclass
class ShareUpdateRequest(MetaModel):
    """Mirrors ``io.metaloom.loom.rest.model.share.ShareUpdateRequest``."""

    #: A new password, in clear. Absent leaves the current one unchanged.
    password: str | None = None
    #: Set true to make the link open. Wins over any password sent in the same request.
    remove_password: bool | None = None
    #: A new expiry date. Absent leaves it unchanged; use clearExpiry to make the link permanent.
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    expires_at: float | None = None
    #: Set true to remove the expiry date, making the link permanent.
    clear_expiry: bool | None = None
    allow_download: bool | None = None
    show_metadata: bool | None = None
    allow_comments: bool | None = None
    allow_reactions: bool | None = None
    allow_annotations: bool | None = None


@dataclass
class SharedAssetListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.share.SharedAssetListResponse``."""

    #: Array which contains the found elements.
    data: list[SharedAssetResponse] = field(default_factory=list)


@dataclass
class SharedAssetResponse(Response):
    """Mirrors ``io.metaloom.loom.rest.model.share.SharedAssetResponse``."""

    #: Original file name.
    filename: str | None = None
    #: Media type, so the viewer knows whether to show a player, an image or a download link.
    mime_type: str | None = None
    #: File size in bytes. Null when the share hides metadata.
    size: int | None = None
    #: Duration in seconds for audio and video. Null otherwise, or when the share hides metadata.
    duration: float | None = None
    #: Pixel width for images and video. Null when the share hides metadata.
    width: int | None = None
    #: Pixel height for images and video. Null when the share hides metadata.
    height: int | None = None
    #: Title, when the material carries one. Null when the share hides metadata.
    title: str | None = None
    #: Description or caption. Null when the share hides metadata.
    description: str | None = None
    #: When the material was added. Null when the share hides metadata.
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    created: float | None = None
