# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.processor``."""

from __future__ import annotations

from dataclasses import dataclass, field

from .base import ListResponse, Model, Response
from .enums import ProcessorCapability, ProcessorState


@dataclass
class ProcessorListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.processor.ProcessorListResponse``."""

    #: Array which contains the found elements.
    data: list[ProcessorResponse] = field(default_factory=list)


@dataclass
class ProcessorResponse(Response):
    """Mirrors ``io.metaloom.loom.rest.model.processor.ProcessorResponse``."""

    #: Stable node id of the processor. The uuid is derived from this and it is the natural key for
    #: correlating REST snapshots with live processor events.
    node_id: str | None = None
    #: Human readable name of the processor node
    name: str | None = None
    #: Priority of the processor node. Higher value means higher priority.
    priority: int | None = None
    #: Host address of the processor node
    host: str | None = None
    #: Current state of the processor node
    state: ProcessorState | None = None
    #: Capabilities of the processor node
    capabilities: list[ProcessorCapability] = field(default_factory=list)
    #: Latest system status reported by the processor node
    system_status: SystemStatusInfo | None = None
    #: Timestamp when the processor was last seen
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    last_seen: float | None = None
    #: Node kinds this worker is permitted to run. An empty/absent whitelist means unrestricted.
    node_whitelist: list[str] = field(default_factory=list)
    #: Node kinds this worker is explicitly forbidden to run. Takes precedence over the whitelist.
    node_blacklist: list[str] = field(default_factory=list)
    #: Whether this worker is known in the durable cortex_instance table. Persisted workers remain listed (and
    #: their restrictions editable) even while offline.
    persisted: bool | None = None


@dataclass
class ProcessorRestrictionUpdateRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.ProcessorRestrictionUpdateRequest``."""

    #: Node kinds this worker is permitted to run. An empty/absent whitelist means unrestricted.
    node_whitelist: list[str] = field(default_factory=list)
    #: Node kinds this worker is explicitly forbidden to run. Takes precedence over the whitelist.
    node_blacklist: list[str] = field(default_factory=list)


@dataclass
class SystemStatusInfo(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.SystemStatusInfo``."""

    #: CPU load percentage (0-100)
    cpu_load: float | None = None
    #: Used memory in bytes
    memory_used: int | None = None
    #: Total memory in bytes
    memory_total: int | None = None
    #: GPU load percentage (0-100)
    gpu_load: float | None = None
    #: I/O load percentage (0-100)
    io_load: float | None = None
    #: Used disk space in bytes
    disk_used: int | None = None
    #: Total disk space in bytes
    disk_total: int | None = None
