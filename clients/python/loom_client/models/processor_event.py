# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.processor.event``."""

from __future__ import annotations

from dataclasses import dataclass
from typing import TYPE_CHECKING

from .base import Model
from .enums import ProcessorEventType

if TYPE_CHECKING:
    from .processor import ProcessorResponse


@dataclass
class ProcessorEventMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.processor.event.ProcessorEventMessage``."""

    #: Multiplexing channel discriminator; always 'PROCESSOR' for processor events
    channel: str | None = None
    #: Processor event type
    type: ProcessorEventType | None = None
    #: Node id of the processor this event refers to (stable UI key)
    node_id: str | None = None
    #: Full processor snapshot (REGISTERED / STATE_CHANGED / STATUS_UPDATED); null for lightweight events
    processor: ProcessorResponse | None = None
    #: Timestamp the processor was last seen (carried on HEARTBEAT)
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    last_seen: float | None = None
