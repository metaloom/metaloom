# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.nodes``."""

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

from .base import Model
from .enums import NodeRegistryEventType


@dataclass
class NodeAvailability(Model):
    """Mirrors ``io.metaloom.loom.rest.model.nodes.NodeAvailability``."""

    #: Where the contract came from: BUILTIN (compiled into Loom) or ANNOUNCED (from a worker)
    source: str | None = None
    available: bool = False
    #: When a worker offering this node was last seen alive (heartbeat-driven)
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    last_seen: float | None = None
    #: When this contract last arrived over the socket. Diagnostic only - see lastSeen for liveness
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    last_announced: float | None = None
    provided_by: list[str] = field(default_factory=list)
    #: Whether the workers offering this node disagree about its contract or version
    version_skew: bool = False


@dataclass
class NodeDescriptorsResponse(Model):
    """Mirrors ``io.metaloom.loom.rest.model.nodes.NodeDescriptorsResponse``."""

    #: Every node contract Loom knows, built-in and announced
    #: NOTE: opaque: node descriptor (loom-shared/node-model)
    node_descriptors: list[dict[str, Any]] = field(default_factory=list)
    #: The content-type vocabulary, including types synthesized from announced ports
    #: NOTE: opaque: content type (loom-shared/node-model)
    content_types: list[dict[str, Any]] = field(default_factory=list)
    #: Runtime state keyed by node id. Absent means 'no fleet information', which reads as available
    availability: dict[str, NodeAvailability] = field(default_factory=dict)


@dataclass
class NodeRegistryEventMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.nodes.NodeRegistryEventMessage``."""

    type: NodeRegistryEventType | None = None
    #: For NODE_AVAILABILITY_CHANGED: the entries that changed, keyed by node id
    availability: dict[str, NodeAvailability] = field(default_factory=dict)
