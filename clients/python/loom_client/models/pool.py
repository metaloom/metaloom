# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.pool``."""

from __future__ import annotations

from dataclasses import dataclass, field

from .base import CreatorEditorResponse, ListResponse, MetaModel


@dataclass
class AssetPoolCreateRequest(MetaModel):
    """Mirrors ``io.metaloom.loom.rest.model.pool.AssetPoolCreateRequest``."""

    #: Unique human-readable name for the pool
    name: str | None = None
    #: Base filesystem path for the pool (only set for filesystem pools)
    fs_path: str | None = None
    #: S3 bucket name (only set for S3 pools)
    s3_bucket: str | None = None
    #: S3 region (only set for S3 pools)
    s3_region: str | None = None
    #: S3 endpoint URL for S3-compatible services (only set for S3 pools)
    s3_endpoint: str | None = None
    #: Free space in bytes (filesystem pools only)
    free_space: int | None = None
    #: Used space in bytes
    used_space: int | None = None


@dataclass
class AssetPoolListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.pool.AssetPoolListResponse``."""

    #: Array which contains the found elements.
    data: list[AssetPoolResponse] = field(default_factory=list)


@dataclass
class AssetPoolResponse(CreatorEditorResponse):
    """Mirrors ``io.metaloom.loom.rest.model.pool.AssetPoolResponse``."""

    #: Unique human-readable name for the pool
    name: str | None = None
    #: Base filesystem path for the pool (only set for filesystem pools)
    fs_path: str | None = None
    #: S3 bucket name (only set for S3 pools)
    s3_bucket: str | None = None
    #: S3 region (only set for S3 pools)
    s3_region: str | None = None
    #: S3 endpoint URL for S3-compatible services (only set for S3 pools)
    s3_endpoint: str | None = None
    #: Free space in bytes (filesystem pools only)
    free_space: int | None = None
    #: Used space in bytes
    used_space: int | None = None


@dataclass
class AssetPoolUpdateRequest(MetaModel):
    """Mirrors ``io.metaloom.loom.rest.model.pool.AssetPoolUpdateRequest``."""

    #: Unique human-readable name for the pool
    name: str | None = None
    #: Base filesystem path for the pool (only set for filesystem pools)
    fs_path: str | None = None
    #: S3 bucket name (only set for S3 pools)
    s3_bucket: str | None = None
    #: S3 region (only set for S3 pools)
    s3_region: str | None = None
    #: S3 endpoint URL for S3-compatible services (only set for S3 pools)
    s3_endpoint: str | None = None
    #: Free space in bytes (filesystem pools only)
    free_space: int | None = None
    #: Used space in bytes
    used_space: int | None = None
