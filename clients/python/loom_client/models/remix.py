# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.remix``."""

from __future__ import annotations

from dataclasses import dataclass, field

from .base import CreatorEditorResponse, ListResponse, MetaModel, Model, Response


@dataclass
class RemixCreateRequest(MetaModel):
    """Mirrors ``io.metaloom.loom.rest.model.remix.RemixCreateRequest``."""

    #: Name of the remix.
    name: str | None = None
    #: Free-text description of what this group of assets has in common.
    description: str | None = None
    #: Uuids of the assets to put in the remix. May be empty.
    asset_uuids: list[str] = field(default_factory=list)
    #: Uuid of the asset to mark as the source. Must be one of assetUuids.
    source_asset_uuid: str | None = None


@dataclass
class RemixListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.remix.RemixListResponse``."""

    #: Array which contains the found elements.
    data: list[RemixResponse] = field(default_factory=list)


@dataclass
class RemixMemberListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.remix.RemixMemberListResponse``."""

    #: Array which contains the found elements.
    data: list[RemixMemberResponse] = field(default_factory=list)


@dataclass
class RemixMemberRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.remix.RemixMemberRequest``."""

    #: Uuids of the assets to add. Adding one that is already a member is not an error.
    asset_uuids: list[str] = field(default_factory=list)
    #: Uuid of the asset to mark as the source, or null to leave the source unchanged.
    source_asset_uuid: str | None = None


@dataclass
class RemixMemberResponse(Response):
    """Mirrors ``io.metaloom.loom.rest.model.remix.RemixMemberResponse``."""

    #: Uuid of the member asset.
    asset_uuid: str | None = None
    #: SOURCE or DERIVED.
    role: str | None = None
    #: User-defined position within the remix. Null sorts last.
    ordinal: int | None = None
    #: Filename of the member asset.
    filename: str | None = None
    #: Mime type of the member asset.
    mime_type: str | None = None
    #: SHA512 content hash of the member asset.
    sha512sum: str | None = None
    #: Size of the member asset in bytes.
    size: int | None = None
    #: When the asset was added to the remix.
    #: NOTE: epoch seconds, not an ISO string -- see parse_instant()
    added: float | None = None
    #: Uuid of the user who added it.
    added_by: str | None = None


@dataclass
class RemixResponse(CreatorEditorResponse):
    """Mirrors ``io.metaloom.loom.rest.model.remix.RemixResponse``."""

    #: Name of the remix.
    name: str | None = None
    #: Free-text description of what this group of assets has in common.
    description: str | None = None
    #: Uuid of the asset the remix is built around, or null when the original is not in the catalogue.
    source_asset_uuid: str | None = None
    #: Number of assets in the remix.
    member_count: int = 0


@dataclass
class RemixUpdateRequest(MetaModel):
    """Mirrors ``io.metaloom.loom.rest.model.remix.RemixUpdateRequest``."""

    #: Name of the remix.
    name: str | None = None
    #: Free-text description of what this group of assets has in common.
    description: str | None = None
    #: Uuid of the asset to mark as the source. Must already be a member.
    source_asset_uuid: str | None = None
