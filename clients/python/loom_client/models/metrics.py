# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.metrics``."""

from __future__ import annotations

from dataclasses import dataclass, field

from .base import Model


@dataclass
class MetricRecord(Model):
    """Mirrors ``io.metaloom.loom.rest.model.metrics.MetricRecord``."""

    #: Scraped Prometheus name of the series, e.g. loom_node_tasks_dispatched_total.
    name: str | None = None
    #: Meter type: COUNTER, GAUGE or TIMER.
    type: str | None = None
    #: Label set of this series, e.g. {\"kind\":\"sha512\",\"state\":\"completed\"}. Empty when the meter is
    #: untagged.
    tags: dict[str, str] = field(default_factory=dict)
    #: Counter total or gauge reading. Null for a timer, which reports count/sum/max/mean instead.
    value: float | None = None
    #: Timer: number of recorded events. Null for counters and gauges.
    count: int | None = None
    #: Timer: total recorded time in seconds. Null for counters and gauges.
    sum_seconds: float | None = None
    #: Timer: longest recorded event in seconds. Null for counters and gauges.
    max_seconds: float | None = None
    #: Timer: mean event duration in seconds (sum/count, 0 when count is 0). Null for counters and gauges.
    mean_seconds: float | None = None


@dataclass
class MetricsResponse(Model):
    """Mirrors ``io.metaloom.loom.rest.model.metrics.MetricsResponse``."""

    #: Server time the snapshot was taken (ISO 8601 instant). Counter deltas between two calls are measured
    #: against this.
    timestamp: str | None = None
    #: One entry per name+tag series, sorted by name then tags.
    metrics: list[MetricRecord] = field(default_factory=list)
