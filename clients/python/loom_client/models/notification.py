# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.notification``."""

from __future__ import annotations

from dataclasses import dataclass, field

from .base import CreatorEditorResponse, ListResponse, Model
from .enums import NotificationType


@dataclass
class NotificationEventMessage(Model):
    """Mirrors ``io.metaloom.loom.rest.model.notification.NotificationEventMessage``."""

    #: Multiplexing channel discriminator; always 'NOTIFICATION' for notification events
    channel: str | None = None
    #: What happened.
    type: NotificationType | None = None
    #: The full notification, so a client can insert it without a refetch.
    notification: NotificationResponse | None = None
    #: The recipient's total unread count after this notification, so the badge is authoritative.
    unread_count: int = 0


@dataclass
class NotificationListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.notification.NotificationListResponse``."""

    #: Array which contains the found elements.
    data: list[NotificationResponse] = field(default_factory=list)
    #: How many unread entries the caller has in total, regardless of paging or the unread filter.
    unread_count: int = 0


@dataclass
class NotificationResponse(CreatorEditorResponse):
    """Mirrors ``io.metaloom.loom.rest.model.notification.NotificationResponse``."""

    #: What happened.
    type: NotificationType | None = None
    #: Whether the recipient has marked this entry as read.
    read: bool = False
    #: ISO8601 timestamp of when the entry was marked read. Absent while unread.
    read_at: str | None = None
    #: Pre-rendered summary line.
    title: str | None = None
    #: Longer detail text.
    body: str | None = None
    #: Uuid of the task this entry is about, when it is about one.
    task_uuid: str | None = None
    #: Uuid of the comment this entry is about, when it is about one.
    comment_uuid: str | None = None
    #: Uuid of the pipeline run this entry is about, when it is about one.
    pipeline_run_uuid: str | None = None
    #: Uuid of the asset this entry is about, when it is about one.
    asset_uuid: str | None = None
    #: Uuid of the group the recipient was reached through, when it was not a direct mention.
    via_group_uuid: str | None = None


@dataclass
class NotificationUpdateRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.notification.NotificationUpdateRequest``."""

    #: Whether the entry has been read. Setting it back to false is allowed - a user may want to revisit
    #: something.
    read: bool | None = None
