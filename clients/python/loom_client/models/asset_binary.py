# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.asset.binary``."""

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

from .base import CreatorEditorResponse, ListResponse, MetaModel, Model


@dataclass
class AssetBinaryCreateRequest(MetaModel):
    """Mirrors ``io.metaloom.loom.rest.model.asset.binary.AssetBinaryCreateRequest``."""

    asset_uuid: str | None = None
    library_uuid: str | None = None
    #: Storage pool holding the bytes. Optional: defaults to the target library's pool, and to the server's
    #: local upload directory when the library has none.
    pool_uuid: str | None = None
    #: Information about the location of the asset in the filesystem.
    filesystem: AssetBinaryFilesystemInfo | None = None
    #: S3 meta information on the asset. (only set when S3 is being utilized).
    s3: AssetS3Meta | None = None


@dataclass
class AssetBinaryFilesystemInfo(Model):
    """Mirrors ``io.metaloom.loom.rest.model.asset.binary.AssetBinaryFilesystemInfo``."""

    #: Current path to the file
    path: str | None = None
    #: Linux filesystem key which identifies the file in the filesystem
    filekey: FileKey | None = None
    #: ISO8601 formatted date string when the asset was last seen online.
    last_seen: str | None = None


@dataclass
class AssetBinaryListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.asset.binary.AssetBinaryListResponse``."""

    #: Array which contains the found elements.
    data: list[AssetBinaryResponse] = field(default_factory=list)


@dataclass
class AssetBinaryReference(Model):
    """Mirrors ``io.metaloom.loom.rest.model.asset.binary.AssetBinaryReference``."""

    uuid: str | None = None
    path: str | None = None


@dataclass
class AssetBinaryResponse(CreatorEditorResponse):
    """Mirrors ``io.metaloom.loom.rest.model.asset.binary.AssetBinaryResponse``."""

    library_uuid: str | None = None
    asset_uuid: str | None = None
    #: The storage pool holding these bytes. Absent when the binary lives in the server's local upload
    #: directory.
    pool_uuid: str | None = None
    #: Which backend holds the bytes: 'filesystem' or 's3'. Derived from the pool; never null.
    storage_type: str | None = None
    #: Information about the location of the asset in the filesystem. Only set for filesystem-backed binaries.
    filesystem: AssetBinaryFilesystemInfo | None = None
    #: S3 meta information on the asset. Only set for S3-backed binaries.
    s3: AssetS3Meta | None = None


@dataclass
class AssetBinaryUpdateRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.asset.binary.AssetBinaryUpdateRequest``."""

    #: Library the binary belongs to. Set this to move the binary into another library; leave it unset to keep
    #: the current one.
    library_uuid: str | None = None
    #: Storage pool holding the bytes. Set this to record that the bytes now live in another pool. Defaults to
    #: the pool of the given library when only libraryUuid is set, and is otherwise left unchanged. Requires
    #: the READ_ASSET_POOL permission.
    pool_uuid: str | None = None
    #: Information about the location of the asset in the filesystem.
    filesystem: AssetBinaryFilesystemInfo | None = None
    #: S3 meta information on the asset. (only set when S3 is being utilized).
    s3: AssetS3Meta | None = None
    #: Additional custom meta properties for the element.
    meta: dict[str, Any] | None = None


@dataclass
class AssetS3Meta(Model):
    """Mirrors ``io.metaloom.loom.rest.model.asset.binary.AssetS3Meta``."""

    #: The id of the s3 bucket in which the asset is stored.
    bucket: str | None = None
    #: The key to the asset within the s3 bucket.
    object_path: str | None = None


@dataclass
class FileKey(Model):
    """Mirrors ``io.metaloom.loom.rest.model.asset.binary.FileKey``."""

    inode: int | None = None
    st_dev: int | None = None
    edate: int | None = None
    edate_nano: int | None = None
