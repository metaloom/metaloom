# GENERATED by tools/generate_models.py -- DO NOT EDIT.
"""Mirrors ``io.metaloom.loom.rest.model.pipeline``."""

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

from .base import CreatorEditorResponse, ListResponse, Model


@dataclass
class PipelineCreateRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineCreateRequest``."""

    #: The name of the pipeline.
    name: str | None = None
    #: The description of the pipeline.
    description: str | None = None
    #: The pipeline definition containing nodes and their configuration.
    definition: dict[str, Any] | None = None
    #: Whether the pipeline is enabled.
    enabled: bool | None = None
    #: The priority of the pipeline.
    priority: int | None = None
    #: Whether the pipeline is in dry-run mode.
    dry_run: bool | None = None
    #: Additional custom meta properties for the element.
    meta: dict[str, Any] | None = None


@dataclass
class PipelineListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineListResponse``."""

    #: Array which contains the found elements.
    data: list[PipelineResponse] = field(default_factory=list)


@dataclass
class PipelineResponse(CreatorEditorResponse):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineResponse``."""

    #: The UUID of the pipeline version this response was rendered from
    version_uuid: str | None = None
    #: The version number this response was rendered from
    version_number: int | None = None
    #: The name of the pipeline
    name: str | None = None
    #: The description of the pipeline
    description: str | None = None
    #: The pipeline definition containing nodes and their configuration
    definition: dict[str, Any] | None = None
    #: Whether the pipeline is enabled
    enabled: bool | None = None
    #: The priority of the pipeline
    priority: int | None = None
    #: Whether the pipeline is in dry-run mode
    dry_run: bool | None = None


@dataclass
class PipelineRunDayStatsRecord(Model):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineRunDayStatsRecord``."""

    #: Calendar day of the bucket (ISO 8601 date, e.g. 2026-07-22).
    date: str | None = None
    #: Number of pipeline runs started on this day (across all pipelines).
    run_count: int = 0
    #: Sum of successfully processed media items of runs started on this day.
    success_count: int = 0
    #: Sum of failed media items of runs started on this day.
    failure_count: int = 0
    #: Sum of skipped media items of runs started on this day.
    skipped_count: int = 0


@dataclass
class PipelineRunItemListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineRunItemListResponse``."""

    #: Array which contains the found elements.
    data: list[PipelineRunItemRecord] = field(default_factory=list)


@dataclass
class PipelineRunItemRecord(Model):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineRunItemRecord``."""

    #: Unique identifier of the pipeline run item.
    uuid: str | None = None
    #: UUID of the pipeline run this item belongs to.
    run_uuid: str | None = None
    #: Sequence number of the item within the run.
    item_seq: int = 0
    #: Path of the media handled by this item.
    media_path: str | None = None
    #: SHA-512 hash of the media, null if not yet computed.
    sha512: str | None = None
    #: Size of the media in bytes, null if unknown.
    size_bytes: int | None = None
    #: Current state: PENDING, RUNNING, SUCCESS, FAILED, SKIPPED.
    state: str | None = None
    #: Error message if the item failed.
    error_message: str | None = None


@dataclass
class PipelineRunListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineRunListResponse``."""

    #: Array which contains the found elements.
    data: list[PipelineRunRecord] = field(default_factory=list)


@dataclass
class PipelineRunRecord(Model):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineRunRecord``."""

    #: Unique identifier of the pipeline run.
    uuid: str | None = None
    #: UUID of the pipeline definition that was executed.
    pipeline_uuid: str | None = None
    #: Version of the pipeline definition at the time of execution.
    pipeline_version: int = 0
    #: When the pipeline run started (ISO 8601).
    started: str | None = None
    #: When the pipeline run finished (ISO 8601), null if still running.
    finished: str | None = None
    #: Current status: PENDING, RUNNING, SUCCESS, FAILED, PARTIAL, CANCELLED.
    status: str | None = None
    #: Total number of media items processed.
    media_count: int = 0
    #: Number of media items processed successfully.
    success_count: int = 0
    #: Number of media items that failed.
    failure_count: int = 0
    #: Number of media items skipped.
    skipped_count: int = 0
    #: Whether this was a dry run (no side effects).
    dry_run: bool = False
    #: Error message if the run failed.
    error_message: str | None = None
    #: Total duration of the run in milliseconds.
    duration_ms: int | None = None
    #: Additional metadata (node stats, etc.).
    meta: dict[str, Any] | None = None


@dataclass
class PipelineRunRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineRunRequest``."""

    #: Optional list of asset UUIDs to run the pipeline against.
    media_uuids: list[str] = field(default_factory=list)
    #: Optional single root directory or file to run against. Enables the source node's differential index-
    #: backed scan. Ignored when pathGlobs is set.
    path: str | None = None
    #: Optional list of path glob patterns used to select media. Forces a full re-walk and takes precedence
    #: over path.
    path_globs: list[str] = field(default_factory=list)
    #: Override the pipeline's dry-run flag for this run.
    dry_run: bool | None = None


@dataclass
class PipelineRunResponse(Model):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineRunResponse``."""

    #: Unique identifier of the started pipeline run.
    run_uuid: str | None = None
    #: Node identifier of the processor that accepted the run.
    processor_node_id: str | None = None
    #: Whether the run was successfully dispatched to a processor.
    dispatched: bool = False
    #: Human-readable status or error message.
    message: str | None = None


@dataclass
class PipelineRunStatsResponse(Model):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineRunStatsResponse``."""

    #: Daily run stats buckets, oldest first. Days without runs are zero-filled.
    daily: list[PipelineRunDayStatsRecord] = field(default_factory=list)


@dataclass
class PipelineUpdateRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineUpdateRequest``."""

    #: The name of the pipeline.
    name: str | None = None
    #: The description of the pipeline.
    description: str | None = None
    #: The pipeline definition containing nodes and their configuration.
    definition: dict[str, Any] | None = None
    #: Whether the pipeline is enabled.
    enabled: bool | None = None
    #: The priority of the pipeline.
    priority: int | None = None
    #: Whether the pipeline is in dry-run mode.
    dry_run: bool | None = None
    #: Additional custom meta properties for the element.
    meta: dict[str, Any] | None = None


@dataclass
class PipelineVersionListResponse(ListResponse):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineVersionListResponse``."""

    #: Array which contains the found elements.
    data: list[PipelineResponse] = field(default_factory=list)


@dataclass
class PipelineVersionRestoreRequest(Model):
    """Mirrors ``io.metaloom.loom.rest.model.pipeline.PipelineVersionRestoreRequest``."""

    #: Optional custom name for the restored version (defaults to original name)
    name: str | None = None
    #: Optional custom description for the restored version (defaults to original description)
    description: str | None = None
