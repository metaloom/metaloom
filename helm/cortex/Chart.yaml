apiVersion: v2
name: cortex
description: >-
  MetaLoom Cortex — the media processing worker. Registers with a Loom backend
  over the processor WebSocket and runs the node tasks Loom dispatches (hashing,
  thumbnails, face detection, transcription, LLM/VLM, …). The image is
  overridable, so this same chart deploys the stock worker or a custom node image.
type: application
# Chart version — bump on chart changes.
#
# 0.2.0 — the meta directory is now always mounted (an emptyDir when
# meta.persistence is disabled). Previously it was left unmounted, which left the
# image's root-owned /meta unwritable by the container's uid 1000 and made every
# filesystem-source run fail while still reporting success.
version: 0.2.0
# The MetaLoom release the default image tracks. Overridable via image.tag.
appVersion: "latest"
home: https://metaloom.io
sources:
  - https://github.com/metaloom
keywords:
  - metaloom
  - cortex
  - media-processing
  - worker
maintainers:
  - name: MetaLoom
    url: https://metaloom.io
