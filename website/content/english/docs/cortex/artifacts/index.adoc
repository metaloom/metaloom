---
title: Artifacts
---

Cortex publishes JVM modules and container deployment assets separately.

== Maven Artifacts

See link:../maven-artifacts/[Maven Artifacts] for the BOM-backed module list and node dependencies.

== Containers

Cortex ships as the `metaloom/cortex-server` image. See link:../containers/[Containers] for Docker,
Docker Compose and Kubernetes `Deployment` examples, and link:../../deployment/[Container Images] for
the full image list.
