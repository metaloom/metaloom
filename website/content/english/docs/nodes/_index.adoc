---
title: Nodes
---

Nodes are the atomic processing steps of MetaLoom. A link:../pipeline/[pipeline] is a graph of nodes,
and link:../cortex/[Cortex] workers execute the individual node tasks that Loom dispatches. Each node
has a unique *kind* (name) used to reference it in a pipeline definition and to route its work to a
worker that can run it.

A node writes its result back to Loom over the REST API — a typed component plus an
`asset_node_result` ledger row. Downstream nodes read a dependency's **output keys**.

Between them the built-in nodes cover any media file type — images, videos, audio and documents. What
is actually analysed for a given asset depends entirely on which nodes the pipeline contains.

Every node page opens with a diagram of that node in a pipeline: what flows in on the left, what comes
out on the right, and a tab per alternative configuration. Ports are labelled with the data type they
carry:

++++
<div class="ml-nodeviz-legend"></div>
++++

The built-in nodes fall into four categories.

[#hashing]
== Hashing & Deduplication

[options="header",cols="2,2,3"]
|===
| Node | Applies to | Purpose
| link:hash/[Hashing] (`md5`, `sha256`, `sha512`, `chunk-hash`) | Any file | Content hashes for identity and deduplication
| link:fingerprint/[Fingerprint] (`fingerprint`) | Video | Perceptual video fingerprint for similarity search
| link:consistency/[Consistency] (`consistency`) | Video, Audio | Detect incomplete / corrupt media
| link:dedup/[Deduplication] (`sha512-dedup`, fingerprint dedup) | Any / Video | Detect and move duplicates
|===

[#extraction]
== Extraction & AI

[options="header",cols="2,2,3"]
|===
| Node | Applies to | Purpose
| link:tika/[Tika] (`tika`) | Any | Extract format metadata + text with Apache Tika
| link:ocr/[OCR] (`ocr`) | Image | Extract text with Tesseract
| link:vlm/[VLM] (`vlm`) | Image | Read images with a vision-language model; olmOCR document transcription
| link:whisper/[Speech-to-Text] (`whisper`) | Video, Audio | Transcribe audio with Whisper
| link:tts/[Text-to-Speech] (`tts`) | Any (from upstream text) | Synthesize narration audio (Orpheus/Kartoffel DE, Kokoro EN)
| link:llm/[LLM Enrichment] (`llm`) | Any | Enrich metadata with a language model
| link:captioning/[Captioning] (`captioning`) | Image, Video | Natural-language captions for images and videos
| link:facedetect/[Face Detection] (`facedetect`) | Image, Video | Detect faces + embeddings
| link:facedescription/[Face Description] (`facedescription`) | Image | Describe detected faces with a vision model
|===

[#media]
== Media Analysis

[options="header",cols="2,2,3"]
|===
| Node | Applies to | Purpose
| link:thumbnail/[Thumbnail] (`thumbnail`) | Video | Contact-sheet preview thumbnails
| link:quality/[Quality] (`quality`) | Image, Video | Resolution / blurriness / bitrate metrics
| link:scene-detection/[Scene Detection] (`scene-detection`) | Video | Optical-flow scene boundaries
|===

[#pipeline]
== Pipeline

[options="header",cols="2,2,3"]
|===
| Node | Applies to | Purpose
| link:filesystem-source/[Filesystem Source] (`filesystem-source`) | — (source) | Enumerate media from disk
| link:filters/[Filters] (`filter-*`) | — (routing) | Route items down PASS/REJECT branches
| link:loom/[Loom Sync] (`loom`) | Any | Batch hash results back to Loom
|===

[#requirements]
== Requirements at a Glance

Most nodes are pure CPU. A few need a native library or an external model service:

[options="header",cols="2,3"]
|===
| Requirement | Nodes
| OpenCV / video4j native runtime | `fingerprint`, `thumbnail`, `quality`, `scene-detection`
| InspireFace native runtime (GPU optional) | `facedetect`
| Whisper runtime (whisper.cpp; GPU optional) | `whisper`
| Tesseract (Tess4J) | `ocr`
| An LLM provider service | `llm`, `facedescription`
| A vision-language model service | `captioning`, `vlm`
| A TTS sidecar (Orpheus/Kartoffel + Kokoro) | `tts`
| CPU only | `md5`/`sha256`/`sha512`/`chunk-hash`, `consistency`, `tika`, `filesystem-source`, `loom`, filters, dedup
|===

Pin the heavy kinds to dedicated or GPU workers via `CORTEX_NODE_WHITELIST` — see
link:../cortex/configuration/[Cortex Configuration].

== Processing Capabilities

What the built-in node set gives you, and which node provides it:

Asset hashing::
link:hash/[`md5`, `sha256`, `sha512`, `chunk-hash`] run in parallel over any file. Hashes are the
basis for exact deduplication and for skipping already-processed assets.

Video fingerprinting::
link:fingerprint/[`fingerprint`] computes a perceptual multi-sector fingerprint using the
`video4j-fingerprint` library. It survives transcoding, resolution changes and minor edits, which
makes near-duplicate detection possible at scale via vector similarity search.

Face detection::
link:facedetect/[`facedetect`] finds faces in images and video with the InspireFace library, returning
bounding boxes, facial embeddings and the optimal focal point for cropped thumbnails.
link:facedescription/[`facedescription`] then describes those faces with a vision model.

Thumbnail generation::
link:thumbnail/[`thumbnail`] renders contact-sheet previews from video frames and source images and
uploads them to Loom as asset binaries.

Metadata extraction::
link:tika/[`tika`] uses Apache Tika to pull structured and unstructured metadata out of any supported
format — EXIF from images, codec information from video, full text from documents.

Scene detection::
link:scene-detection/[`scene-detection`] splits video into scenes with an optical-flow detector,
producing cut timestamps that can drive per-scene thumbnails or clips.

OCR and speech-to-text::
link:ocr/[`ocr`] extracts text from images and document pages with Tesseract;
link:vlm/[`vlm`] transcribes document pages with a vision-language model such as olmOCR, which keeps
the layout — tables, columns, formulas — that plain glyph recognition flattens;
link:whisper/[`whisper`] transcribes audio and video soundtracks with the Whisper model.

AI captioning and enrichment::
link:captioning/[`captioning`] generates natural-language descriptions of both images and videos with
a vision-language model — images from a still frame (SmolVLM), videos from their temporal content
(Qwen2.5-VL), optionally as a per-scene timeline — and link:llm/[`llm`] enriches metadata through an
LLM provider such as Ollama.

Text-to-speech::
link:tts/[`tts`] turns upstream text — an link:llm/[`llm`] summary, a caption, or a translated
transcript — into narration audio, using Orpheus/Kartoffel for German and Kokoro for English behind a
small HTTP sidecar.

Consistency checking::
link:consistency/[`consistency`] validates that stored metadata — hashes, dimensions, codec
information — still matches the file on disk, flagging inconsistencies for review or remediation.

== Authoring Your Own Node

Cortex is designed to be extended, but there is no drop-in plugin mechanism: the node set of a Cortex
process is wired at build time through Dagger modules, so the shipped `cortex-server` image serves
exactly the built-in kinds listed above. Running a node of your own means *building your own Cortex
instance* that includes it. A worker announces the node kinds its factory has registered when it
registers with Loom, so a custom instance becomes dispatchable as soon as it connects, and it can run
alongside the stock workers — which keep serving the built-in kinds.

A custom node in Java is three steps:

. Extend `AbstractMediaNode<YourOptions>`.
. Implement `name()`, `isProcessable()` and `compute()`.
. Expose the node from a Dagger module, include that module in your instance's node collection, and
  register the node kind with the pipeline node factory.

The `examples/` directory of the source repository carries the complete, working setup rather than a
sketch: `cortex-custom-node` holds the node with its options, payload and Dagger module;
`cortex-custom` assembles the daemon that includes that module and connects to a Loom backend; and
`cortex-python` implements a minimal worker in Python that speaks the wire protocol directly, for
logic you would rather not write in Java. See link:../cortex/examples/[Cortex Examples] for the code
and the build and run commands.
