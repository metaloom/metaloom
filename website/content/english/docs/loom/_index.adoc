---
title: Loom
weight: 5
---

MetaLoom // *Loom* is the media asset management server at the heart of the MetaLoom platform.
It exposes a REST API over HTTP, a GraphQL API, and a gRPC API for managing digital media assets and all related entities.

== Components

* *REST API* — Full CRUD for all entities. See link:rest-api[REST API Reference].
* *GraphQL API* — Flexible, nested queries over the asset model, with an interactive explorer. See link:graphql-api[GraphQL API].
* *gRPC API* — Binary RPC over HTTP/2 on its own port, for integrations that want a generated client. Coordinates for the Java client are in link:maven-artifacts[Maven Artifacts].
* *MCP Server* — Connect Claude Desktop, an IDE or your own AI agent to your library over the Model Context Protocol. See link:mcp[MCP Server].
* *Java Client* — Typed HTTP client for JVM applications. See link:java-client[Java Client Usage].
* *Python Client* — Typed client for Python, with no third-party dependencies. See link:python-client[Python Client Usage].
* *Authentication* — JWT bearer tokens and optional OAuth2. See link:authentication[Authentication].
* *Binary Storage* — Uploading and downloading media, and choosing between disk and S3 per library. See link:binary-storage[Binary Storage].
* *Configuration* — YAML config file and environment variables. See link:configuration[Configuration].
* *Metrics* — The Prometheus scrape endpoint and the `loom_*` catalog. See link:metrics[Metrics].
* *Search Indices* — Inspecting, reindexing and syncing the search, face and duplicate indices. See link:search-indices[Search Indices].
* *Storage Usage* — How much space each kind of content uses, and how full each storage backend is. See link:storage[Storage Usage].
* *Sharing* — Send a link to somebody without a Loom account, and collect their feedback on it. See link:sharing[Sharing].
* *Artifacts* — Maven coordinates, containers and the Helm chart. See link:artifacts[Artifacts].
* *Features* — Assets, users, groups, roles, tags, pipelines and more. See link:features[Features].
* *Chat & AI Agent* — Conversational assistant with Sessions, Skills, Memory and an optional coding sandbox. See link:chat[Chat & AI Agent].
* *Examples* — Code snippets from the `/examples` module. See link:examples[Examples].
