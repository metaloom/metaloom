<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>io.metaloom.loom.db</groupId>
		<artifactId>loom-db</artifactId>
		<version>1.0.0-SNAPSHOT</version>
	</parent>

	<artifactId>loom-db-jooq</artifactId>
	<name>MetaLoom // Loom :: DB :: JOOQ</name>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<db.username>postgres</db.username>
		<db.password>finger</db.password>
		<!-- Overridable so a codegen run can be pointed at a scratch directory
		     instead of overwriting the checked-in sources. -->
		<jooq.output.dir>src/jooq/java</jooq.output.dir>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>io.metaloom</groupId>
				<artifactId>bom</artifactId>
				<type>pom</type>
				<scope>import</scope>
				<version>${project.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>io.metaloom.loom</groupId>
			<artifactId>loom-common</artifactId>
		</dependency>
		<dependency>
			<groupId>io.metaloom.loom.db</groupId>
			<artifactId>loom-db-api</artifactId>
		</dependency>
		<dependency>
			<groupId>io.metaloom.loom.db</groupId>
			<artifactId>loom-db-flyway</artifactId>
		</dependency>

		<!-- JOOQ -->
		<dependency>
			<groupId>org.jooq</groupId>
			<artifactId>jooq</artifactId>
			<version>${jooq.version}</version>
		</dependency>
		<dependency>
			<groupId>javax.persistence</groupId>
			<artifactId>javax.persistence-api</artifactId>
		</dependency>

		<!-- JDBC / Connection Pooling support -->
		<dependency>
			<groupId>com.mchange</groupId>
			<artifactId>c3p0</artifactId>
		</dependency>

		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
		</dependency>

		<!-- Test -->
		<dependency>
			<groupId>io.metaloom.loom.db</groupId>
			<artifactId>loom-db-api-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- JooqModule.r2dbcConnectionFactory resolves the driver through the r2dbc SPI at
		     runtime. Without an implementation on the classpath ConnectionFactories.get fails
		     with "Available drivers: [ None ]". -->
		<dependency>
			<groupId>io.r2dbc</groupId>
			<artifactId>r2dbc-postgresql</artifactId>
			<version>0.8.10.RELEASE</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<profiles>
		<profile>
			<activation>
				<property>
					<name>generate</name>
				</property>
			</activation>

			<build>
				<plugins>
					<plugin>
						<groupId>org.codehaus.gmaven</groupId>
						<artifactId>groovy-maven-plugin</artifactId>
						<version>2.1.1</version>
						<executions>
							<execution>
								<id>testcontainer-start</id>
								<phase>generate-sources</phase>
								<goals>
									<goal>execute</goal>
								</goals>
								<configuration>
									<source>
										db = new org.testcontainers.containers.PostgreSQLContainer("postgres:latest")
											.withUsername("${db.username}")
											.withDatabaseName("postgres")
											.withPassword("${db.password}");
										db.start();
										project.properties.setProperty('db.url', db.getJdbcUrl());
										project.properties.setProperty('testcontainer.containerid', db.getContainerId());
										project.properties.setProperty('testcontainer.imageName', db.getDockerImageName());
									</source>
								</configuration>
							</execution>
							<execution>
								<id>testcontainer-stop</id>
								<phase>test</phase>
								<goals>
									<goal>execute</goal>
								</goals>
								<configuration>
									<source>
										containerId = "${testcontainer.containerid}"
										imageName = "${testcontainer.imageName}"
										println("Stopping testcontainer $containerId - $imageName")
										org.testcontainers.utility.ResourceReaper
											.instance()
											.stopAndRemoveContainer(containerId, imageName);
									</source>
								</configuration>
							</execution>
						</executions>

						<dependencies>
							<dependency>
								<groupId>org.testcontainers</groupId>
								<artifactId>postgresql</artifactId>
								<version>1.17.6</version>
							</dependency>
						</dependencies>
					</plugin>
					<plugin>
						<groupId>org.flywaydb</groupId>
						<artifactId>flyway-maven-plugin</artifactId>
						<version>9.14.1</version>
						<executions>
							<execution>
								<?m2e ignore?>
								<phase>generate-sources</phase>
								<goals>
									<goal>migrate</goal>
								</goals>
							</execution>
						</executions>

						<configuration>
							<url>${db.url}</url>
							<user>${db.username}</user>
							<password>${db.password}</password>
							<validateMigrationNaming>true</validateMigrationNaming>
							<locations>
								<location>
									filesystem:../flyway/src/main/resources/db/migration</location>
							</locations>
						</configuration>
						<dependencies>
							<dependency>
								<groupId>org.postgresql</groupId>
								<artifactId>postgresql</artifactId>
								<version>${postgres.driver.version}</version>
							</dependency>
						</dependencies>
					</plugin>

					<plugin>
						<groupId>org.jooq</groupId>
						<artifactId>jooq-codegen-maven</artifactId>
						<version>${jooq.version}</version>

						<executions>
							<execution>
								<id>jooq-codegen</id>
								<phase>generate-sources</phase>
								<goals>
									<goal>generate</goal>
								</goals>
							</execution>
						</executions>

						<dependencies>
							<dependency>
								<groupId>org.postgresql</groupId>
								<artifactId>postgresql</artifactId>
								<version>${postgres.driver.version}</version>
							</dependency>
							<dependency>
								<groupId>org.jooq</groupId>
								<artifactId>jooq</artifactId>
								<version>${jooq.version}</version>
							</dependency>
							<dependency>
								<groupId>org.jooq</groupId>
								<artifactId>jooq-meta</artifactId>
								<version>${jooq.version}</version>
							</dependency>
							<dependency>
								<groupId>org.jooq</groupId>
								<artifactId>jooq-codegen</artifactId>
								<version>${jooq.version}</version>
							</dependency>
							<dependency>
								<groupId>io.metaloom.loom.db</groupId>
								<artifactId>loom-db-jooq-gen</artifactId>
								<version>${project.version}</version>
							</dependency>
						</dependencies>

						<configuration>
							<jdbc>
								<driver>org.postgresql.Driver</driver>
								<url>${db.url}</url>
								<user>${db.username}</user>
								<password>${db.password}</password>
							</jdbc>

							<generator>
								<strategy>
									<name>io.metaloom.loom.db.jooq.codegen.LoomJooqStrategy</name>
								</strategy>
								<generate>
									<daos>false</daos>
								</generate>
								<database>
									<name>
										org.jooq.meta.postgres.PostgresDatabase</name>
									<includes>.*</includes>
									<!-- text_search / text_search_en / trgm_text are GENERATED ALWAYS AS ...
									     STORED columns maintained by PostgreSQL. They must never appear in an
									     INSERT/UPDATE, so they are kept out of the generated code entirely.
									     jOOQ also has no tsvector binding, so they would generate as Object.
									     The Postgres search provider addresses them by name via DSL.field(),
									     the same technique AssetComponentDaoImpl uses.
									     includeExcludeColumns is required: by default includes/excludes
									     apply to tables and other objects, not to columns. -->
									<excludes>.*\.text_search.*|.*\.trgm_text</excludes>
									<includeExcludeColumns>true</includeExcludeColumns>
									<inputSchema>public</inputSchema>
									<outputSchema>public</outputSchema>
									<unsignedTypes>false</unsignedTypes>
									<forcedTypes>
										<forcedType>
											<userType>io.vertx.core.json.JsonObject</userType>
											<converter>io.metaloom.loom.db.jooq.converter.JsonObjectConverter</converter>
											<!-- pipeline_node_task.outputs holds node outputs and is read
											     as a JsonObject like meta, so it needs the same converter.
											     pipeline_node_task.previews holds the opt-in debugging
											     renderings of those outputs and is read the same way.
											     asset_node_result.result_ref points at the rows a node wrote. -->
											<includeExpression>.*\.meta.*|.*\.outputs|.*\.previews|.*\.definition|.*\.result_ref</includeExpression>
										</forcedType>
										<forcedType>
											<userType>io.vertx.core.json.JsonArray</userType>
											<converter>io.metaloom.loom.db.jooq.converter.JsonArrayConverter</converter>
											<!-- chat.messages holds the chat transcript as a JSON array -->
											<includeExpression>chat\.messages</includeExpression>
										</forcedType>
										<forcedType>
											<userType>java.lang.String</userType>
											<converter>io.metaloom.loom.db.jooq.converter.JsonStringConverter</converter>
											<!-- node_descriptor.descriptor is a serialized NodeDescriptor that goes
											     straight to and from Jackson. Mapping it to JsonObject would re-parse
											     and re-encode the whole document on every read for no gain, and would
											     drag a Vert.x type into loom-db-api. -->
											<includeExpression>node_descriptor\.descriptor</includeExpression>
										</forcedType>
										<!-- The three pipeline status/state columns are deliberately VARCHAR -
										     a Postgres enum would need a migration for every new value - so the
										     vocabulary is enforced in Java instead. These converters are the only
										     boundary at which a bad string can be caught, and they reject one
										     naming the column and the value rather than passing it to the UI. -->
										<forcedType>
											<userType>io.metaloom.loom.api.pipeline.PipelineRunStatus</userType>
											<converter>io.metaloom.loom.db.jooq.converter.PipelineRunStatusConverter</converter>
											<includeExpression>pipeline_run\.status</includeExpression>
										</forcedType>
										<forcedType>
											<userType>io.metaloom.loom.api.pipeline.PipelineRunKind</userType>
											<converter>io.metaloom.loom.db.jooq.converter.PipelineRunKindConverter</converter>
											<includeExpression>pipeline_run\.kind</includeExpression>
										</forcedType>
										<forcedType>
											<userType>io.metaloom.loom.api.pipeline.NodeTaskState</userType>
											<converter>io.metaloom.loom.db.jooq.converter.NodeTaskStateConverter</converter>
											<includeExpression>pipeline_node_task\.state</includeExpression>
										</forcedType>
										<forcedType>
											<userType>io.metaloom.loom.api.pipeline.RunItemState</userType>
											<converter>io.metaloom.loom.db.jooq.converter.RunItemStateConverter</converter>
											<includeExpression>pipeline_run_item\.state</includeExpression>
										</forcedType>
									</forcedTypes>
								</database>
								<target>
									<packageName>io.metaloom.loom.db.jooq</packageName>
									<directory>${jooq.output.dir}</directory>
								</target>
							</generator>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<build>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>add-source</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>add-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>${project.basedir}/src/jooq/java/</source>
							</sources>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
