* Implement spec/plans/imagegen-node.md.
 - start with ideogram-sidecar and test it out. I want to see a generated image
* Improve Dagger node collection but not the factory, so Loom rejects those runs with 503 (Make it dynamic?)
 - Introduce SPI for discovery? / better: Register nodes in cortex on an internal node registry?

* Figure out caption vs video-caption (Why two?)
* Update the loom helm chart
*  implement TTS / ASR solution - test it first